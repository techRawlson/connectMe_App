import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const StudentAttendanceRecordRenderTable = ({ headers, data }) => {
  return (
    <View>
      <Text>StudentAttendanceRecordRenderTable</Text>
    </View>
  )
}

export default StudentAttendanceRecordRenderTable

const styles = StyleSheet.create({})